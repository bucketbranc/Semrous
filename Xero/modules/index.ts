import {init as a, manifest as b} from './history_flooder';
import {init as c, manifest as d}  from './metalpipe';
import {init as e, manifest as f} from './gemi2';
import {init as g, manifest as h} from './brokenscreen';

export const modules =  {
     history_flooder: {init: a, manifest: b},
     metalpipe: {init: c, manifest: d},
     gemi2: {init: e, manifest: f},
     brokenscreen: {init: g, manifest:h}
}
